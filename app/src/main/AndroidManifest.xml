<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.alice.yahahun">

    <!-- 🔧 Optional device feature declarations -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <!-- ✅ Required permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Yahan Hun"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LocationApp"
        tools:targetApi="31">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAq9dKmEMN0uD9EycuofLOuFoaeQJOUwpo" />
        <!-- ✅ Launcher activity: GreetingActivity -->
        <activity
            android:name="com.alice.yahanhun.GreetingActivity"
            android:label="Yahan Hun"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ Used after greeting or if not signed in -->
        <activity android:name="com.alice.yahanhun.LoginActivity" android:exported="true" />

        <!-- ✅ Used during first-time onboarding -->
        <activity android:name="com.alice.yahanhun.InformationActivity" android:exported="true" />
        <activity android:name="com.alice.yahanhun.PasswordActivity" android:exported="true" />

        <!-- ✅ Main map activity -->
        <activity
            android:name="com.alice.yahanhun.HomeActivity"
            android:exported="true">
            <!-- 🔗 Deep link support -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="yahanhun" android:host="track" />
            </intent-filter>
        </activity>

        <!-- ❓ Optional legacy MainActivity (not a launcher anymore) -->
        <activity android:name="com.alice.yahanhun.MainActivity" android:exported="true" />
    </application>

</manifest>